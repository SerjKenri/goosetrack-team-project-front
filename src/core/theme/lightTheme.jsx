const { colorsLight } = require("./colors");
const { font } = require("./fonts");
const { space } = require("./space");

const lightTheme = {
  space,
  font,
  color: colorsLight,
};

export { lightTheme };
