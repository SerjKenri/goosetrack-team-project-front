const { colorsDark } = require("./colors");
const { font } = require("./fonts");
const { space } = require("./space");

const darkTheme = {
  space,
  font,
  color: colorsDark,
};

export { darkTheme };
